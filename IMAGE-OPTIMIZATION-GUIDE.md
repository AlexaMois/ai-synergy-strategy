# 📸 Руководство по оптимизации изображений

## ✅ Что уже реализовано

### 1. Компонент OptimizedImage
Все изображения на сайте используют оптимизированный компонент `OptimizedImage` с поддержкой:
- ✅ **Lazy Loading** - изображения загружаются по мере прокрутки страницы
- ✅ **WebP формат** - автоматическая поддержка WebP с fallback на оригинальный формат
- ✅ **Responsive Images** - атрибуты `sizes` для адаптивной загрузки
- ✅ **Priority Loading** - критичные изображения (Hero) загружаются сразу
- ✅ **Fade-in эффект** - плавное появление изображений после загрузки

### 2. Оптимизация сборки (Vite)
В `vite.config.ts` настроены:
- ✅ Разделение кода на чанки (code splitting)
- ✅ Inline небольших ассетов (<4kb) как base64
- ✅ Оптимизация зависимостей

### 3. Preload критичных ресурсов
В `index.html` добавлены preload теги для портрета на главном экране.

---

## 🚀 Следующий шаг: Создание WebP версий

Чтобы завершить оптимизацию, создайте WebP версии изображений:

### Быстрый способ (онлайн):
1. Откройте [Squoosh.app](https://squoosh.app/)
2. Загрузите файлы из `src/assets/`
3. Выберите формат WebP, качество 85%
4. Скачайте и сохраните в `src/assets/` с расширением `.webp`

**Список файлов для конвертации:**
- `alexandra-portrait.jpg` → `alexandra-portrait.webp`
- `brush-accent-1.png` → `brush-accent-1.webp`
- `brush-accent-2.png` → `brush-accent-2.webp`
- `brush-stroke.jpg` → `brush-stroke.webp`
- `n-pattern.png` → `n-pattern.webp`

### Автоматический способ (командная строка):

**Вариант 1: ImageMagick**
```bash
cd src/assets

# Конвертация всех изображений
for file in *.{jpg,png}; do
  magick convert "$file" -quality 85 "${file%.*}.webp"
done
```

**Вариант 2: cwebp (Google)**
```bash
# Установка (macOS)
brew install webp

# Конвертация
cd src/assets
for file in *.{jpg,png}; do
  cwebp -q 85 "$file" -o "${file%.*}.webp"
done
```

---

## 📊 Ожидаемые результаты

После создания WebP версий:
- **Уменьшение размера**: -30 до -50% от оригинала
- **Экономия трафика**: ~200-500 KB на загрузку страницы
- **Улучшение LCP**: +20-40% (быстрее появление главного контента)
- **PageSpeed Score**: +10-15 баллов

---

## 🔍 Проверка результатов

Используйте инструменты:
- [PageSpeed Insights](https://pagespeed.web.dev/)
- [WebPageTest](https://www.webpagetest.org/)
- Chrome DevTools → Lighthouse

Обратите внимание на метрики:
- **LCP** (Largest Contentful Paint) - время загрузки главного контента
- **CLS** (Cumulative Layout Shift) - стабильность верстки
- **FID** (First Input Delay) - скорость реакции на действия

---

## 💡 Дополнительные оптимизации (опционально)

### Создание нескольких размеров изображений
Для идеальной оптимизации создайте версии разных размеров:

```bash
# Для alexandra-portrait.jpg
magick convert alexandra-portrait.jpg -resize 640x -quality 85 alexandra-portrait-640.webp
magick convert alexandra-portrait.jpg -resize 1024x -quality 85 alexandra-portrait-1024.webp
magick convert alexandra-portrait.jpg -resize 1920x -quality 85 alexandra-portrait-1920.webp
```

Затем используйте srcSet в коде:
```tsx
<OptimizedImage
  src={alexandraPortrait}
  alt="Александра Моисеева"
  sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 40vw"
  srcSet="
    /assets/alexandra-portrait-640.webp 640w,
    /assets/alexandra-portrait-1024.webp 1024w,
    /assets/alexandra-portrait-1920.webp 1920w
  "
/>
```

---

## 📝 Техническая документация

Подробная документация находится в `OPTIMIZATION.md`.

**Контакты для вопросов:**
- Техническая документация: `OPTIMIZATION.md`
- Компонент: `src/components/OptimizedImage.tsx`
- Утилиты: `src/utils/imagePreloader.ts`
