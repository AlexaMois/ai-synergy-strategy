## Что делаю

Перестраиваю финальный блок «Приглашение · 04» в `src/pages/neurostylist/NeurostylistPage.tsx` так, чтобы добавленный референс (5 образов: «Предпринимательница / Городская дерзость / Женская сила / Магнетизм / Публичный лидер») органично вписался в текущую плум-сцену — как на скриншоте `ChatGPT_Image_6_мая_2026_г._23_37_37.png`, а не белым пятном.

## Шаги

1. **Подготовка изображения**
   - Копирую `user-uploads://image-170.png` в `src/assets/neurostylist/lookbook-5.jpg`.
   - В рантайме картинка ляжет на тёмный плум-фон карточек с двумя приёмами тонировки, чтобы убрать «белое пятно» исходника:
     - `mix-blend-mode: luminosity` + лёгкий `saturate(0.9)` `contrast(1.05)` — сохраняем форму образов, но впитываем плум-температуру фона.
     - Поверх — дополнительный градиент `linear-gradient(180deg, rgba(42,22,32,0.55) 0%, rgba(26,9,16,0.0) 35%, rgba(26,9,16,0.0) 65%, rgba(26,9,16,0.85) 100%)` для бесшовного слияния с секцией сверху и снизу.

2. **Новый layout секции 04**
   - Заголовок «ТВОЙ ОБРАЗ / уже можно собрать» сдвигаю влево-вверх (как на референсе), а справа добавляю короткий supporting-текст:
     «Пять образов, из которых складывается твоя цельная картинка.»
   - Под заголовком — **5 вертикальных карточек-«полароидов»** в одну линию (`grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`, на мобилке — горизонтальный скролл `snap-x`).

3. **Карточка-look (×5)**
   - Контейнер: `aspect-[3/5]`, `border-radius: 18px`, тонкая рамка `1px solid rgba(247,237,227,0.08)`, мягкая тень `0 30px 60px -30px rgba(0,0,0,0.7)`.
   - Фон карточки: плум-градиент `linear-gradient(180deg,#2A1422 0%,#1A0910 100%)`.
   - Внутри — **обрезок** общего изображения lookbook-5: вместо 5 отдельных файлов использую один файл, спозиционированный через `background-image` + `background-size: 500% 100%` и `background-position-x: 0% / 25% / 50% / 75% / 100%` для каждой карточки. Это даёт идеальное совпадение с референсом без генерации 5 отдельных ассетов.
   - Сверху изображения — оверлей `mix-blend-mode: multiply` плумового цвета `rgba(42,14,30,0.35)` для единой температуры.
   - В верху карточки (поверх фото): крупная цифра `1…5` шрифтом `Cormorant Garamond italic` цветом rose-gold (`#D4956A`, opacity 0.9).
   - Под цифрой — uppercase-label rose-gold (`tracking-[0.32em]`, 11px):
     `ПРЕДПРИНИМАТЕЛЬНИЦА`, `ГОРОДСКАЯ ДЕРЗОСТЬ`, `ЖЕНСКАЯ СИЛА`, `МАГНЕТИЗМ`, `ПУБЛИЧНЫЙ ЛИДЕР`.
   - Под label — короткое описание ivory `rgba(247,237,227,0.78)`, 13px, 2 строки:
     1. Топ на одно плечо + брюки-палаццо
     2. Рубашка под пояс / корсетный пояс + широкие брюки
     3. Жилет в талию + прямые брюки
     4. Боди с открытой спиной + брюки со стрелками
     5. Платье с открытой спиной

4. **CTA**
   - Кнопки `Собрать для меня образ` и `Заполнить анкету` сохраняю, переношу под ряд карточек по центру (как на референсе), отступы оставляю прежние.

5. **Адаптив**
   - ≥1024px: 5 карточек в ряд, заголовок слева, supporting-текст справа.
   - 640–1023px: 3 карточки в ряд + 2 во второй (или горизонтальный скролл со snap).
   - <640px: горизонтальный snap-скролл, карточки `min-w-[60vw]`.

6. **Согласованность с остальным лендингом**
   - Использую существующие токены/шрифты (`Cormorant Garamond`, `Outfit`, rose-gold `#D4956A`, ivory `#F7EDE3`), без новых цветов.
   - Spotlight-радиал на фоне секции остаётся, но уменьшаю его opacity до 0.28, чтобы карточки читались.

## Файлы

- `src/assets/neurostylist/lookbook-5.jpg` — копия `user-uploads://image-170.png`.
- `src/pages/neurostylist/NeurostylistPage.tsx` — переработка секции 04 (строки ~867–950) + небольшие scoped-классы `.ns-look-card`, `.ns-look-img` в существующем `<style>`-блоке страницы.

## Что НЕ трогаю

- Структуру квиза, остальные секции, CTA-логику, шрифты в других блоках, цветовые токены проекта.
