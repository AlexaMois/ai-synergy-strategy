## Что делаю

В hero на главной заменяю иллюстрацию «человечка за компом» на ваше фото (портрет с ноутбуком) с полностью удалённым задним фоном.

## Шаги

1. Копирую загруженное фото `user-uploads://ChatGPT_Image_25_мая_2026_г._17_58_24.png` в `src/assets/alexandra-hero.png`.
2. Через `imagegen--edit_image` с `transparent_background=true` создаю версию без фона → `src/assets/alexandra-hero-transparent.png`.
3. В `src/pages/NewHome.tsx`:
   - Меняю импорт `heroCharacter` с `@/assets/sketches/hero-character.png` на новый прозрачный PNG.
   - Подгоняю `alt` («Александра Моисеева») и при необходимости размер контейнера, чтобы фото красиво легло в правую колонку бирюзового hero-блока (без фона, с лёгкой тенью).

Старый файл `hero-character.png` не удаляю — он может использоваться где-то ещё.

## Нюансы

- Фон у фото удалится полностью (прозрачный PNG), так что бирюзовый цвет hero-блока будет проступать вокруг силуэта.
- Если после удаления фона по краям останется ореол, повторю обработку с уточнённым промптом.
